name: release

on:
  create:

jobs:
  release:
    permissions:
      contents: write
      packages: write
    uses: cat-of-summer/GitHub_workflows/.github/workflows/release.yml@main
    secrets:
      PACKAGIST_API_TOKEN: ${{ secrets.PACKAGIST_API_TOKEN }}
