<!-- DOCGEN:START -->
# FaqPage

## Файлы

- [Question.php](Question.php.md)

<!-- DOCGEN:END -->

`Question` — вложенная схема одного вопроса-ответа, используемая полем `questions` родительской схемы `FaqPage` (`../FaqPage.php`). Создаётся автоматически при `FaqPage::fill(['questions' => [...]])`, отдельно почти не используется.
