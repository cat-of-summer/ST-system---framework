<!-- DOCGEN:START -->
# ItemList

## Файлы

- [ListItem.php](ListItem.php.md)

<!-- DOCGEN:END -->

`ListItem` — вложенная схема одного элемента списка, используемая полем `items` родительской схемы `ItemList` (`../ItemList.php`). Создаётся автоматически при `ItemList::fill(['items' => [...]])`.
